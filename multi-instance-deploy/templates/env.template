# .env — {{INSTANCE}} Sovereign Node
# Generated by create-instance.sh v3.6

# ── Instance Identity ──
TZ={{TZ}}

# ── Authentication ──
AUTH_LOGIN=admin
AUTH_PASSWORD=admin

# ── Agent Profile ──
A0_SET_agent_profile={{PROFILE}}

# ── Matrix MCP Integration ──
# Configure in UI: Settings > MCP/A2A tab
# MHS Homeserver: http://{{MHS_IP}}:8008
# Matrix Identity: @{{INSTANCE}}:{{MHS}}.cybertribe.com

# ── Model Configuration (override as needed) ──
# A0_SET_chat_model_provider=openrouter
# A0_SET_chat_model_name=anthropic/claude-sonnet-4
# A0_SET_chat_model_ctx_length=200000
# A0_SET_utility_model_provider=openrouter
# A0_SET_utility_model_name=anthropic/claude-sonnet-4
# A0_SET_utility_model_ctx_length=200000
# A0_SET_embeddings_model_provider=openai
# A0_SET_embeddings_model_name=text-embedding-3-small
